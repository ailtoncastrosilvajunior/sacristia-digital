<div class="space-y-6">
  <div>
    <%= link_to "← #{@competencia.label}", admin_competencia_mensal_path(@competencia), class: "text-sm text-stone-500 hover:text-stone-700 mb-2 block" %>
    <h1 class="text-2xl font-semibold text-stone-800">Novo evento</h1>
    <p class="text-stone-500 mt-1">Adicione um evento à escala de <%= @competencia.label %></p>
  </div>

  <div class="bg-white rounded-xl border border-stone-200 shadow-sm p-6 max-w-2xl">
    <%= form_with model: @evento, url: admin_evento_escala_index_path, html: { class: "space-y-5" } do |f| %>
      <%= f.hidden_field :competencia_mensal_id %>
      <% if @evento.errors.any? %>
        <div class="rounded-lg bg-red-50 p-4 text-red-800 text-sm">
          <p class="font-medium mb-2"><%= pluralize(@evento.errors.count, "erro") %> impediram o cadastro:</p>
          <ul class="list-disc list-inside">
            <% @evento.errors.full_messages.each do |msg| %>
              <li><%= msg %></li>
            <% end %>
          </ul>
        </div>
      <% end %>

      <%= render "form_fields", f: f %>

      <div class="flex gap-3 pt-4">
        <%= f.submit "Criar evento", class: "px-4 py-2.5 bg-slate-600 hover:bg-slate-700 text-white font-medium rounded-lg transition-colors cursor-pointer" %>
        <%= link_to "Cancelar", admin_competencia_mensal_path(@competencia), class: "px-4 py-2.5 border border-stone-300 text-stone-700 hover:bg-stone-50 font-medium rounded-lg transition-colors" %>
      </div>
    <% end %>
  </div>
</div>
