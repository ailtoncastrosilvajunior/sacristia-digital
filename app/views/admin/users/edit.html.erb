<% content_for :title, "Editar usuário — Admin" %>
<div class="max-w-3xl mx-auto">
  <div class="mb-6">
    <%= link_to "← #{@user.email}", admin_user_path(@user), class: "text-sm font-medium text-sky-700 hover:text-sky-900" %>
  </div>
  <h1 class="text-2xl font-bold text-stone-900 mb-6">Editar usuário</h1>
  <%= render "form", user: @user %>
</div>
